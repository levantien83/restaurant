 
<h1><small>Thank you for orderring </small><%= @food_item.name %>!</h1>
<h1>
  <small>Please pay </small>
  <%= number_to_currency(@food_item.price + 20e3, :locale => :vi, precision: 0) %>
  <small> (including 20.000 VND delivery charge).</small>
</h1><br>

  <h3>Order info:</h3>
  Name: <%= @order.name %><br>

  Address: <%= @order.address %><br>
  Phone: <%= @order.phone %><br>
  <%= @order.created_at.strftime("%A, %B %d, %Y, %H:%M") %><br>
<%= link_to 'Menu', menu_path, class: "btn btn-primary btn-lg" %>